in the index page... will have search.. and limit it by 5-10 results
that way it will be easier to maintain and produce search results.

<div class="index-cards">
	<h3>public recipes</h3>
		<% @public_recipes.each do |r| %>
			<div class="recipe-card">
				<%=link_to r.title, recipe_path(r)%>
				<%=image_tag last_image(r.images), class: "first-recipe-image" %>
				<h6><%=r.summary %></h6>
			</div>
		<% end %>
</div>



<% if session[:user_id] %>
<h3>recipes</h3>
	<div class="index-cards">
		<% @recipes[0].each do |r| %>
			<div class="recipe-card">
				<%=link_to r.title, recipe_path(r)%>
				<%=image_tag last_image(r.images), class: "first-recipe-image" %>
				<h6><%=r.summary %></h6>
			</div>

		<% end %>
		</ul>
	</div>

<% end %>
				